GRAPHITE_FLAGS="-ftree-vectorize -floop-interchange -ftree-loop-distribution -floop-strip-mine -floop-block"

CFLAGS="${CFLAGS} ${GRAPHITE_FLAGS}"
CXXFLAGS="${CXXFLAGS} ${GRAPHITE_FLAGS}"
LDFLAGS="${LDFLAGS} ${GRAPHITE_FLAGS}"
